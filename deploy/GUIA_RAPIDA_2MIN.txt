🚀 DEPLOY VMIX SCHEDULE 44 EN TRUENAS - GUÍA RÁPIDA (2 MINUTOS)
═══════════════════════════════════════════════════════════════════════

📍 UBICACIÓN ACTUAL: http://192.168.192.44 (pantalla abierta)

⏱️  TIEMPO ESTIMADO: 15 minutos de ejecución


PASO 1: ABRIR SHELL (30 segundos)
═══════════════════════════════════════════════════════════════════════

1. En TrueNAS abierto: SISTEMA → SHELL
2. Haz click en "SHELL" en la esquina inferior derecha
3. Se abre una consola negra


PASO 2: COPIAR SCRIPT INSTALACIÓN (1 minuto)
═══════════════════════════════════════════════════════════════════════

Abre archivo: INSTRUCCIONES_SHELL_TRUENAS.txt

Busca la sección:
  "PASO 2: COPIA Y PEGA ESTO EN LA SHELL"

Copia TODO (desde #!/bin/bash hasta echo "✅ INSTALACIÓN COMPLETADA")

Pega en la shell negra y presiona ENTER


PASO 3: ESPERAR (5-10 minutos)
═══════════════════════════════════════════════════════════════════════

El script:
  ✓ Crea directorio /opt/vmix-schedule-44/
  ✓ Instala Python 3.9
  ✓ Instala Nginx
  ✓ Crea entorno virtual
  ✓ Instala dependencias Flask

Verás mucho output. Espera a que termine.

Cuando veas: "✅ INSTALACIÓN COMPLETADA"
  → Sigue al PASO 4


PASO 4: NGINX (1 minuto)
═══════════════════════════════════════════════════════════════════════

Copia desde INSTRUCCIONES_SHELL_TRUENAS.txt:
  Sección: "PASO 3: CONFIGURAR NGINX"

Pega en shell y ENTER


PASO 5: SYSTEMD (1 minuto)
═══════════════════════════════════════════════════════════════════════

Copia desde INSTRUCCIONES_SHELL_TRUENAS.txt:
  Sección: "PASO 4: CREAR SERVICIO SYSTEMD"

Pega en shell y ENTER


PASO 6: INICIAR TODO (2 minutos)
═══════════════════════════════════════════════════════════════════════

Pega estos comandos UNO A UNO:

sudo systemctl daemon-reload
sudo systemctl enable nginx
sudo systemctl start nginx
sudo systemctl enable vmix-schedule-44
sudo systemctl start vmix-schedule-44


PASO 7: VERIFICAR (30 segundos)
═══════════════════════════════════════════════════════════════════════

Pega:
sudo systemctl status vmix-schedule-44

Debe mostrar: "active (running)" en verde

Si muestra error:
  → Ver logs: sudo tail -50 /opt/vmix-schedule-44/logs/api.log


PASO 8: ACCEDER (10 segundos)
═══════════════════════════════════════════════════════════════════════

Abre en navegador:
http://192.168.192.44/ui/

Debe mostrar:
  ✅ Interfaz oscura
  ✅ Título "vMix Schedule 44"
  ✅ Punto verde o rojo (vMix status)
  ✅ Botón "AUTO: OFF"


✅ ¡LISTO! El sistema está operativo.


PRÓXIMOS PASOS DE OPERACIÓN
═══════════════════════════════════════════════════════════════════════

1. Agregar evento de prueba:
   - Pestaña "Eventos"
   - Día: "Monday"
   - Hora: "14:30"
   - Input: "1"
   - Click "Agregar"

2. Activar automatización:
   - Click botón "AUTO: OFF" → cambia a "AUTO: ON"

3. Monitorear:
   - Pestaña "Logs" muestra acciones en tiempo real

4. Agregar eventos reales según necesites


COMANDOS ÚTILES (En Shell de TrueNAS)
═══════════════════════════════════════════════════════════════════════

# Ver logs en vivo
sudo tail -f /opt/vmix-schedule-44/logs/api.log

# Reiniciar servicio
sudo systemctl restart vmix-schedule-44

# Ver estado
sudo systemctl status vmix-schedule-44

# Probar API
curl -s http://127.0.0.1:5000/api/status | python3 -m json.tool

# Ver configuración
cat /opt/vmix-schedule-44/data/vMix_Schedule_44_Contenidos_Config.json


🎯 RESULTADO FINAL
═══════════════════════════════════════════════════════════════════════

URL:              http://192.168.192.44/ui/
Backend:          http://192.168.192.44/api/
Ubicación:        /opt/vmix-schedule-44/
Puerto Frontend:  80 (Nginx)
Puerto Backend:   5000 (Flask)
Autostart:        Sí (systemd)


⚠️  SI ALGO FALLA
═══════════════════════════════════════════════════════════════════════

"vMix Desconectado":
  → curl -s http://192.168.192.140:8098/api/ | head -5
  → Si falla: vMix no está accesible

"No se conecta":
  → sudo systemctl status nginx
  → sudo systemctl restart nginx

"Servicio se cae":
  → sudo tail -100 /opt/vmix-schedule-44/logs/api.log
  → Buscar errores

"Error de permisos":
  → sudo chown -R root:root /opt/vmix-schedule-44
  → sudo chmod -R 755 /opt/vmix-schedule-44


═══════════════════════════════════════════════════════════════════════
Total tiempo: ~15 minutos
Dificultad: Baja (solo copiar y pegar)
Resultado: Sistema profesional en producción
═══════════════════════════════════════════════════════════════════════
